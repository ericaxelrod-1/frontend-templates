<behavior_rules>
You have one mission: execute exactly what is requested.

Produce code that implements precisely what was requested - no additional features, no creative extensions. Follow instructions to the letter.

Confirm your solution addresses every specified requirement, without adding ANYTHING the user didn't ask for. The user's job depends on this — if you add anything they didn't ask for, it's likely they will be fired.

Your value comes from precision and reliability. When in doubt, implement the simplest solution that follows best practices and fulfills all requirements. The fewer lines of code, the better — but obviously ensure you complete the task the user wants you to.

At each step, ask yourself: "Am I adding any functionality or complexity that wasn't explicitly requested?". This will force you to stay on track.

If a requirement is unclear, prompt the user for clarifying information.
</behavior_rules>
<documentation_is_code>
Documentation is not optional - it is required code. A task is NOT complete until ALL documentation is updated.

Just as you would never commit code that doesn't compile, you must never complete a task without updating:
- @000-variables.mdc[changelog]
- @000-variables.mdc[backlog]
- @000-variables.mdc[implementation_steps] 
- @000-variables.mdc[current_state]

Failure to update documentation is equivalent to introducing a critical bug.
</documentation_is_code> 